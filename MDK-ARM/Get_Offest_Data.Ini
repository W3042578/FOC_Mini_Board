FUNC void Get_Offest_Data(void) {
  int idx;
 
  exec("log > Offest_Data.log");
  for (idx = 0; idx < 256; idx++) {
    printf ("Encoder_Line_Offest_Table[%d] = %d\n", idx, Encoder_Line_Offest_Table[idx]);
  }
  exec("log off");
}